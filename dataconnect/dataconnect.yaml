specVersion: "v1"
serviceId: "paigeapp"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "paigeapp-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
